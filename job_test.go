package watcompute

/*
func TestDag(t *testing.T) {
	d := Event{}
	for i := 0; i < 5; i++ {
		m := Manifest{fmt.Sprintf("Manifest %d", i), fmt.Sprintf("%d", i), 1, false, Plugin{}, Model{}, []DataSource{}, []DataSource{}}
		d.AddManifest(m)
	}
	fmt.Println(d)
}
*/
